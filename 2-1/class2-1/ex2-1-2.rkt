;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex2-1-1) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; 习题2.1.2
;; 在RrScheme中计算(sqrt 4)、(sqrt 2)和(sqrt -1)。再查
;; 明DrScheme是否包含计算一个角度的正切值的运算。



;; 答：

;; --------------------------------------------------

;; 计算(sqrt 4)
(sqrt 4) ;; 

;; 计算(sqrt 2)
(sqrt 2) ;; 

;; 计算(sqrt -1)
(sqrt -1) ;; 

;; --------------------------------------------------

;; DrScheme包含计算一个角度的正切值的运算：
(tan 90) ;; 
(tan 0) ;; 
(tan 30) ;; 
(tan 60) ;; 